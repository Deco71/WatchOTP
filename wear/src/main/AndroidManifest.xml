<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".StoreApplication"
        android:allowBackup="false"
        android:icon="@mipmap/wearotp_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/wearotp_icon_round"
        android:theme="@android:style/Theme.DeviceDefault" >
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />


        <activity
            android:name="com.decoapps.wearotp.wear.MainActivity"
            android:exported="true"
            android:taskAffinity="">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".OTPUpdates"
            android:exported="true">
            <intent-filter>
                <!-- listeners receive events that match the action and data filters -->
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" />
                <data android:host="*" />
                <data android:pathPrefix="/create-token" />
                <data android:scheme="wear" />
                <data android:host="*" />
                <data android:pathPrefix="/delete-token" />
            </intent-filter>
        </service>

    </application>

</manifest>